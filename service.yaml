apiVersion: v1
kind: Service
metadata:
  name: todo-app-service
spec:
  type: NodePort
  selector:
    app: TODO
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
